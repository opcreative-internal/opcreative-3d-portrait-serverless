name: Build and Push GHCR

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'handler.py'
      - 'verify_imports.py'
      - 'pipeline_v11_depth_umeyama.py'
      - 'pipeline_v14.py'
      - 'pipeline_v15.py'
      - 'pipeline_v16.py'
      - 'pipeline_v17.py'
      - 'pipeline_v18.py'
      - 'pipeline_v19.py'
      - 'pipeline_v21.py'
      - 'module_a_retune.py'
      - 'module_wrap_texture.py'
      - 'module_wrap_texture_v19.py'
      - 'module_wrap_texture_v21.py'
      - 'module_autofront_v21h.py'
      - 'module_bgremove_v21h.py'
      - 'module_e_texture.py'
      - 'module_e_engrave.py'
      - 'module_e_ssle.py'
      - 'module_f_graft.py'
      - '.github/workflows/build-and-push.yml'
  workflow_dispatch:

# Serialize builds: cancel any in-progress build when a new push lands. Prevents
# the V19-era race where 3 parallel builds all pushed to :latest and whichever
# finished LAST won -- often the earliest-started one with a stale Dockerfile.
concurrency:
  group: ghcr-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL || true
          docker system prune -af || true
          df -h

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/cowork-3d-v14:latest
            ghcr.io/${{ github.repository_owner }}/cowork-3d-v14:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

# V18b trigger 20260725-090036

# V18c trigger 20260725-093212

# V18d trigger 20260725-100906

# V18e trigger 20260725-114228

# V19 trigger 20260725-131924

# V19b race-fix trigger 20260725-135745

# V19b race-fix trigger 20260725-141310

# V20 trigger 20260725-145337

# V20 trigger 20260725-152217

# V20.1 trigger 20260725-155844

# V21b trigger 20260725-191637

# V21c free-rotation trigger 20260725-200000

# V21e auto-TripoSG trigger 20260725-213800

# V21e-fix triposg /opt path 20260725-215000

# V21e-log log_tail_on_error 20260725-215500

# V21f Fable v3 TripoSG import chain fix 20260725-220500

# V21f-v2 typeguard 2.13.3 (jaxtyping constraint) 20260725-223500

# V21f-v3 remove grep fatal + patch pipeline_triposg 20260725-224500

# V21f-v4 pin diffusers 0.30.3 + transformers 4.44.2 + peft 0.11.1 for torch 2.4 20260725-225000

# V21f-v4b retrigger after concurrency cancel 20260725-225500

# V21h Fable v3 mesh-cache: expose stageA URL for wrap-only re-render 20260726-002500

# V21h-fix Fable v3 Bugs A+B+C+D: true ortho basis + z-buffer + auto-front + BGremove 20260726-004500

# V21h.1 Fable v3 root-cause: xatlas mp.Queue drain fix + bgremove out[0][0] + log_tail 300 20260726-095500

# V21h.2 Fable v3 hidden-bug: unwrap_uvs str-guard + mediapipe Tasks API + blazeface + numpy<2 20260726-122500

# V21h.3 Fable v3 issues-1-2-4-5: autofront gate+prior+keylight + subject-alpha bbox + GLB 2048 preview + integrity check + BG-emit-RGBA 20260726-141000

# V21h.4 Fable v3 landmark-sim fold-in: FaceLandmarker photo+skin-tinted-render, s/t fold into scale_xy+img_center, warn-skip fallback 20260726-155000
